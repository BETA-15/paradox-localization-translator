# v0.11.58 GUI応答性監査

大量Mod・大型localization・多数バックアップを前提に、13タブの主要な重処理経路を確認した。

| タブ | 確認した主な処理 | v0.11.58時点 |
|---|---|---|
| 翻訳 / キュー | LLM翻訳、差分準備、単体/一括上書き | 翻訳worker維持。差分マニフェスト/欠損解析と上書きI/Oをバックグラウンド化 |
| 中国語基準翻訳 | LLM翻訳、差分準備、上書き | 翻訳worker維持。差分準備をバックグラウンド化 |
| QA / 比較編集 | YAML読込、QA全件解析、大量行描画 | 解析をworkerへ移動。Treeviewはチャンク描画 |
| 差分調査 | 2ファイル解析、全キー比較、大量行描画 | 解析をworkerへ移動。Treeviewはチャンク描画 |
| 翻訳検索 | Mod横断検索 | 既存search worker / generation制御を確認 |
| 用語集 | 自動用語作成、日本語Mod/本体からの大量取込 | 既存worker経路を確認 |
| モデル / 接続 | モデル一覧取得、ベンチマーク | 既存worker経路を確認 |
| 未翻訳監視 | ファイル統計、欠損走査、探索LLM | 既存monitor workerを確認 |
| 翻訳状況 | Mod走査、Mod分類、日本語化Mod判定、本体キー70%例外、キャッシュ復元 | 調査worker維持。起動時キャッシュ復元をworker化。検索をデバウンス |
| 翻訳状況 / 判定ログ | 全日本語候補×元Modの再計算、ログ書出し | 生成/書出しをworker化。70%本体キー例外を回帰確認 |
| 総合診断 | 全Mod走査、キー索引、関係判定、修復、前回結果復元 | 既存診断/修復worker維持。起動時の前回結果JSON復元をworker化 |
| バックアップ復元 | 数百〜数千履歴走査、翻訳状況から復元先解決、復元コピー | 一覧走査/解決/復元をworker化。重複更新要求をcoalesce |
| 設定 | データルート変更時の大量キャッシュ/バックアップコピー | コピーをworker化。切替後の巨大翻訳状況キャッシュも同期読込しない |
| 使い方 | 静的表示 | 重処理なし |

## スレッド安全方針

- Tkinter Widget / Variable の更新はメインスレッドで行う。
- workerはファイル走査、YAML/JSON解析、比較、コピー、LLM/API処理を担当する。
- workerの結果は `self.events` を経由して `_poll_events()` でUIへ反映する。
- 大量Treeview行はQA/差分調査で分割描画し、長時間1回のUIループ占有を避ける。
- バックアップ一覧は更新要求が重なっても複数の重い走査を並列起動しない。

## 回帰確認

- `python -m py_compile app/main.py app/translator_core.py`: PASS
- Xvfb GUI smoke: 13タブ生成、v0.11.58起動/終了: PASS
- 1.2秒の模擬バックアップ走査中にTk heartbeatが約0.19秒で実行: PASS
- 6000キーQA解析中にTk heartbeatが約0.31秒で実行し、QA完了: PASS
- 日本語化Mod候補側→ゲーム本体キー一致率 80%: -100点 / 自動除外: PASS
- 同一致率 69%: 本体和訳修正例外の減点なし: PASS
